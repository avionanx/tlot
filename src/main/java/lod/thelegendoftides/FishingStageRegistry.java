package lod.thelegendoftides;

import org.legendofdragoon.modloader.registries.MutableRegistry;

public class FishingStageRegistry extends MutableRegistry<FishingStage> {
  public FishingStageRegistry() { super(Tlot.id("fishing_stage")); }
}
